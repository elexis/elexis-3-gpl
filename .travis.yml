---
language: java

install: mvn  -V -DskipTests=true -Dmaven.javadoc.skip=true  -Dtycho.localArtifacts=ignore -B validate

script: mvn  -V --quiet clean verify -Dtycho.localArtifacts=ignore && find . -name "*2site*.zip"
  
notifications:
  email:
  - niklaus.giger@member.fsf.org
jdk:
  - oraclejdk8

cache:
  directories:
  - $HOME/.m2
